<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.damars.damardjati_submissiondicodingke1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DamarDjati_SubmissionDicodingke1"
        android:fullBackupContent="@xml/backup_descriptor">
        <activity android:name=".Introduction"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"/>
        <activity android:name=".forDetailUser" android:parentActivityName=".MainActivity"/>
        <activity android:name=".Aboutcreator" android:parentActivityName=".MainActivity"/>
    </application>

</manifest>